import styled from 'styled-components/native';

const CenteredView = styled.Text`
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
`

export default CenteredView;
